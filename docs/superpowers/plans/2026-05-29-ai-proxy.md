# AI 加菜后端代理 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给「✨ 一句话加菜谱」加一个 Supabase Edge Function 代理，让线上（GitHub Pages）也能用 AI，密钥藏在服务端，调用仅限已登录用户。

**Architecture:** 新增 Edge Function `ai-recipe`（持有 Anthropic key、默认 `verify_jwt` 挡未登录、调 Anthropic 并解析出干净 JSON）。前端 `aiGenerate()` 改为带登录 token 调这个函数，拿到 5 个字段后照旧组装 `rec` 并云同步。前端 4 文件仍零构建、零依赖、纯静态。

**Tech Stack:** Supabase Edge Functions（Deno / TypeScript）、原生 JS、Anthropic Messages API。

**测试说明:** 本项目无测试框架。每个任务的「验证」= 代码审查 + 浏览器手动验证（可用 Claude Preview 临时 stub `fetch` / `sb.auth.getSession` 来跑前端分支）。Edge Function 的真实端到端联调需用户拿到 Anthropic key 并部署后进行，已在 Task 4 单列。

**关键路径与约定:**
- 仓库根目录（worktree）：`C:\Users\Leonz\OneDrive\桌面\Personal\Projects\EverydayRecipe\.claude\worktrees\laughing-yonath-1fd6d3`
- 分支：`feature/ai-proxy`（已从 main 切出）
- 提交信息全中文，**不要加 `Co-Authored-By: Claude` trailer**。
- 前端 Supabase 常量已在 `app.js` 第 3–5 行：`SUPABASE_URL='https://dmrxlnvgwjqiwkjgsgcp.supabase.co'`、`SUPABASE_KEY='<anon key>'`、`sb=supabase.createClient(...)`。

---

## File Structure

- **新增** `supabase/functions/ai-recipe/index.ts` —— Edge Function 本体。职责：CORS、校验 `desc`、用服务端 key 调 Anthropic、解析出 `{name,en,type,p,ing}` 返回。独立后端文件，不进前端加载链。
- **修改** `app.js` —— 仅改 `aiGenerate()`（第 168–184 行）：改为调代理、加 token、删前端 prompt、按状态码分错误提示。
- **修改** `index.html` —— 第 78 行 `.ai-note` 过时说明文案。

---

## Task 1: Edge Function `ai-recipe`

**Files:**
- Create: `supabase/functions/ai-recipe/index.ts`

- [ ] **Step 1: 写函数本体**

创建 `supabase/functions/ai-recipe/index.ts`，完整内容如下：

```typescript
// Supabase Edge Function：AI 一句话加菜代理。
// verify_jwt 默认开启，未登录请求由网关 401 拦下，函数内无需再校验 JWT。

const CORS = {
  "Access-Control-Allow-Origin": "*",
  "Access-Control-Allow-Headers": "authorization, apikey, content-type",
  "Access-Control-Allow-Methods": "POST, OPTIONS",
};

Deno.serve(async (req: Request) => {
  if (req.method === "OPTIONS") {
    return new Response(null, { status: 204, headers: CORS });
  }

  const json = (body: unknown, status = 200) =>
    new Response(JSON.stringify(body), {
      status,
      headers: { ...CORS, "Content-Type": "application/json" },
    });

  let desc = "";
  try {
    const body = await req.json();
    desc = typeof body?.desc === "string" ? body.desc.trim() : "";
  } catch {
    return json({ error: "请求体格式错误" }, 400);
  }
  if (!desc) return json({ error: "缺少描述" }, 400);

  const apiKey = Deno.env.get("ANTHROPIC_API_KEY");
  if (!apiKey) return json({ error: "服务端未配置 API key" }, 500);

  const prompt =
    `根据这句话描述一道菜，只输出一个 JSON 对象，不要任何多余文字或 markdown 代码块。` +
    `字段：name(中文菜名), en(英文或拼音名), type(中文小分类，如 炸物/盖饭/汤面/卷类/甜品/小吃), ` +
    `p(数组，从这些蛋白质里选适用的：pork,chicken,beef,sea,egg,tofu；纯素或主要是淀粉就用 ["tofu"]), ` +
    `ing(中文主要材料，用顿号、分隔，6到10样)。描述：${desc}`;

  let aResp: Response;
  try {
    aResp = await fetch("https://api.anthropic.com/v1/messages", {
      method: "POST",
      headers: {
        "x-api-key": apiKey,
        "anthropic-version": "2023-06-01",
        "content-type": "application/json",
      },
      body: JSON.stringify({
        model: "claude-sonnet-4-20250514",
        max_tokens: 1000,
        messages: [{ role: "user", content: prompt }],
      }),
    });
  } catch {
    return json({ error: "无法连接 AI 服务" }, 502);
  }

  if (!aResp.ok) return json({ error: "AI 服务出错" }, 502);

  const data = await aResp.json();
  const text = (data.content || [])
    .map((b: { text?: string }) => b.text || "")
    .join("")
    .trim()
    .replace(/```json/g, "")
    .replace(/```/g, "")
    .trim();

  let obj: { name?: string; en?: string; type?: string; p?: string[]; ing?: string };
  try {
    obj = JSON.parse(text);
  } catch {
    return json({ error: "AI 返回格式异常" }, 502);
  }

  return json({
    name: obj.name ?? "",
    en: obj.en ?? "",
    type: obj.type ?? "",
    p: Array.isArray(obj.p) ? obj.p : [],
    ing: obj.ing ?? "",
  });
});
```

- [ ] **Step 2: 静态检查（若本机有 Deno）**

Run: `deno check supabase/functions/ai-recipe/index.ts`
Expected: 无类型错误。
（若本机无 Deno：跳过此步，改为人工通读确认 —— 函数对每条返回路径都带 `CORS` 头；`desc` 非字符串/空走 400；Anthropic 非 2xx 或解析失败走 502；成功返回 5 字段。Deno.serve / Deno.env 是 Edge 运行时全局，无需 import。）

- [ ] **Step 3: 提交**

```bash
git add supabase/functions/ai-recipe/index.ts
git commit -m "feat: 新增 ai-recipe Edge Function（AI 加菜后端代理）"
```

---

## Task 2: 前端 `aiGenerate()` 改为调代理

**Files:**
- Modify: `app.js`（`aiGenerate()`，约第 168–184 行）

- [ ] **Step 1: 替换 `aiGenerate()` 整个函数**

把 `app.js` 中现有的 `async function aiGenerate(){ ... }` 整体替换为：

```javascript
async function aiGenerate(){
  const desc=aiInput.value.trim();if(!desc)return;
  const {data:{session}}=await sb.auth.getSession();
  if(!session){showLogin();return;}
  aiStatus.textContent='正在生成「'+desc+'」…';
  try{
    const resp=await fetch(`${SUPABASE_URL}/functions/v1/ai-recipe`,{
      method:'POST',
      headers:{'Content-Type':'application/json','Authorization':'Bearer '+session.access_token,'apikey':SUPABASE_KEY},
      body:JSON.stringify({desc})
    });
    if(!resp.ok){
      aiStatus.textContent=resp.status===401?'登录已过期，请重新登录':'AI 生成失败，请换个描述再试';
      return;
    }
    const obj=await resp.json();
    let p=(Array.isArray(obj.p)?obj.p:[]).filter(x=>PSET.includes(x));if(p.length===0)p=['tofu'];
    const rec={id:'c'+Date.now(),cat:'custom',sub:obj.type||'AI 生成',flag:'⭐',name:obj.name||desc,en:obj.en||'',p,ing:obj.ing||'',custom:true};
    customRecipes.unshift(rec);saveCustom();
    aiInput.value='';aiStatus.textContent='已加入「⭐ 我的菜谱」：'+rec.name;
    document.querySelectorAll('.chip').forEach(c=>c.classList.remove('active'));document.querySelector('[data-filter="all"]').classList.add('active');
    render('all');closePanel(aiOv,aiP);
    setTimeout(()=>{const card=[...document.querySelectorAll('.card')].find(c=>c.dataset.name===rec.name);if(card){card.classList.add('flash');card.scrollIntoView({behavior:'smooth',block:'center'});setTimeout(()=>card.classList.remove('flash'),1400);}},150);
  }catch(e){aiStatus.textContent='网络错误，请稍后再试';}
}
```

要点（与旧版的差异）：
- 新增取 `session` 并在无 session 时 `showLogin()` 返回。
- `fetch` 目标由 `api.anthropic.com` 改为 `${SUPABASE_URL}/functions/v1/ai-recipe`，请求头加 `Authorization` + `apikey`，body 改为 `{desc}`。
- **删除了前端的 prompt 字符串**（已搬到函数）。
- 错误处理改为按 `resp.status` 分支 + catch 网络错误。
- PSET 过滤、`rec` 组装、`saveCustom`、render/flash 一字不改。

- [ ] **Step 2: 浏览器验证（成功路径，stub 代理）**

启动预览（`.claude/launch.json` 的 `static`，端口 8765），打开页面。因为没有真实登录/key，用 Claude Preview 的 eval 临时 stub `sb.auth.getSession` 和 `window.fetch` 来跑通前端逻辑：

```javascript
// 在 preview_eval 里执行：
(()=>{
  sb.auth.getSession=async()=>({data:{session:{access_token:'fake'}}});
  window.fetch=async(url,opt)=>({ok:true,status:200,json:async()=>({name:'测试炸鸡',en:'Test Chicken',type:'炸物',p:['chicken'],ing:'鸡腿、面粉、盐'})});
  document.getElementById('aiInput').value='测试';
  return 'stubbed';
})()
```
然后点击「→」(`#aiGo`) 或 eval `aiGenerate()`。
Expected: 页面切到「全部」，「测试炸鸡」作为一张卡片出现在「⭐ 我的菜谱」区域并 flash 高亮；`#aiStatus` 显示「已加入「⭐ 我的菜谱」：测试炸鸡」。验证完刷新页面清掉 stub。

- [ ] **Step 3: 浏览器验证（错误分支）**

刷新后重新 stub，模拟 401 与 502/网络错误：

```javascript
// 401：
(()=>{ sb.auth.getSession=async()=>({data:{session:{access_token:'fake'}}}); window.fetch=async()=>({ok:false,status:401}); document.getElementById('aiInput').value='测试'; aiGenerate(); })()
// 预期 #aiStatus = 「登录已过期，请重新登录」

// 502：
(()=>{ window.fetch=async()=>({ok:false,status:502}); document.getElementById('aiInput').value='测试'; aiGenerate(); })()
// 预期 #aiStatus = 「AI 生成失败，请换个描述再试」

// 网络异常：
(()=>{ window.fetch=async()=>{throw new Error('net');}; document.getElementById('aiInput').value='测试'; aiGenerate(); })()
// 预期 #aiStatus = 「网络错误，请稍后再试」
```
Expected: 三种情况 `#aiStatus` 文案分别如上注释。验证完刷新清掉 stub。

- [ ] **Step 4: 提交**

```bash
git add app.js
git commit -m "feat: AI 加菜改走后端代理（带登录 token、按状态码分错误提示）"
```

---

## Task 3: 更新 index.html 说明文案

**Files:**
- Modify: `index.html`（第 78 行 `.ai-note`）

- [ ] **Step 1: 替换 `.ai-note` 文案**

把 `index.html` 第 78 行：

```html
<div class="ai-note">注：AI 生成需联网且仅在 Claude 预览中可用；部署到 GitHub Pages 后此功能会失效（需后端代理才能调用 AI），但登录、收藏、买菜清单的云同步在 GitHub Pages 上正常工作。</div>
```

替换为：

```html
<div class="ai-note">注：AI 加菜已通过 Supabase 后端代理支持线上使用（需登录）。生成的菜谱会加入「⭐ 我的菜谱」并云端同步。</div>
```

- [ ] **Step 2: 浏览器验证**

刷新预览页面，登录后点「✨ 一句话加菜谱」打开面板。
Expected: 面板底部说明显示新文案「注：AI 加菜已通过 Supabase 后端代理支持线上使用（需登录）…」，无旧的「部署到 GitHub Pages 后此功能会失效」字样。

- [ ] **Step 3: 提交**

```bash
git add index.html
git commit -m "docs: 更新 AI 加菜说明文案（已支持线上）"
```

---

## Task 4: 部署与端到端联调（需用户参与）

**前置:** 用户已有 Anthropic API key、已安装 Supabase CLI。

> 此任务在用户拿到 key 前无法完成，作为交付清单留给用户/后续执行。实现阶段（Task 1–3）不依赖它。

- [ ] **Step 1: 部署函数**

```bash
supabase functions deploy ai-recipe --project-ref dmrxlnvgwjqiwkjgsgcp
```
Expected: 部署成功，输出函数地址 `https://dmrxlnvgwjqiwkjgsgcp.supabase.co/functions/v1/ai-recipe`。

- [ ] **Step 2: 设置密钥**

```bash
supabase secrets set ANTHROPIC_API_KEY=sk-ant-...
```
Expected: secret 设置成功。

- [ ] **Step 3: 未登录访问应被拒（验证防滥用）**

```bash
curl -i -X POST https://dmrxlnvgwjqiwkjgsgcp.supabase.co/functions/v1/ai-recipe -H "Content-Type: application/json" -d "{\"desc\":\"测试\"}"
```
Expected: HTTP 401（无合法 JWT，被网关拦下）。

- [ ] **Step 4: 登录后端到端**

在线上站点登录，打开「✨ 一句话加菜谱」，输入「韩式甜辣炸鸡」，点「→」。
Expected: 生成一道菜加入「⭐ 我的菜谱」、flash 高亮、云端同步；刷新后仍在。
